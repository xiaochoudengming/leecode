class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> Optional[ListNode]:
        passed=set()
        while headA:
            passed.add(headA)
            headA.next

        while headB:
            if headB in passed:
                return headB
            headB=headB.next
        return None
