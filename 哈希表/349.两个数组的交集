class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        counts={}
        for i in nums1:
            counts[i]=counts.get(i,0)+1

        result=[]
        for j in nums2:
            if j in counts:
                result.append(j)
                del counts[j]
        return result
